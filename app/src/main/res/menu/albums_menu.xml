<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/menu_album_activity_layout_album_songs_button"
        android:title="@string/menu_song_list"
        app:showAsAction="ifRoom"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/menu_albums_activity_layout_currently_playing_button"
        android:title="@string/menu_currently_playing"
        app:showAsAction="ifRoom"/>


</menu>